"""
Given an integer array nums of length n,
you want to create an array ans of length 2n such that:
where ans[i] == nums[i] and ans[i + n] == nums[i]
for 0 <= i < n (0-indexed).
"""
def concat_array(nums):
    
    return nums + nums

print(concat_array([1, 2, 1]))
        
    
